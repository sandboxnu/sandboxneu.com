/* Typography is supposed to help set up fonts and sensible CSS defaults using normalize.css */
import Typography from "typography"

const typography = new Typography({
  baseFontSize: "18px",
  baseLineHeight: 1.666,
  headerFontFamily: ["Brandon", "sans-serif"],
  bodyFontFamily: ["Brandon", "sans-serif"],
})

export default typography
